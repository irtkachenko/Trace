services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    image: trace-app:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - PORT=3000
